// if(state==in_squotes)--> rm quotes, no expansion or anything , everything is literal a text
// if(state==in_dquotes)-->should remove the dquotes and expand variables(it doesnt execute the commands)

//  if(state==in_dquotes){
// 	if(tok.type == env)
// 		//expand the var
// 	//write the string
// }

// if(state == GENERAL) --> rm spaces 